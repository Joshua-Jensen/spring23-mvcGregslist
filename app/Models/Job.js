import { generateId } from "../Utils/generateId.js";

export class Jobs {
    constructor(data) {
        this.id = generateId()
        this.title = data.title
        this.salary = data.salary
        this.
    }
}